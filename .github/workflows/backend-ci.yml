name: Backend CI

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main, dev]

jobs:
  lint:
    name: Ruff Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Poetry
        uses: abatilo/actions-poetry@v3
        with:
          poetry-version: 1.8.3

      - name: Install dependencies
        run: poetry install --no-interaction --no-root --with dev

      - name: Run ruff (lint)
        run: poetry run ruff check . --output-format=github

      - name: Run mypy (type checking)
        run: poetry run mypy cartaos
        
      - name: Security check (safety)
        run: |
          pip install safety
          poetry export --dev --without-hashes -f requirements.txt -o requirements.txt
          safety check --file=requirements.txt
          
      - name: Check API documentation
        run: |
          poetry install --with docs
          cd docs && make html SPHINXOPTS="-W --keep-going -n" && cd ..
        continue-on-error: true

  test:
    name: PyTest + Coverage
    runs-on: ubuntu-latest
    needs: lint
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Poetry
        uses: abatilo/actions-poetry@v3
        with:
          poetry-version: 1.8.3

      - name: Install dependencies
        run: poetry install --no-interaction --no-root --with dev

      - name: Install dev tools explicitly
        run: poetry run pip install pytest mypy

      - name: Force pure Python coverage.py
        run: "poetry run pip install --no-binary :all: coverage"

      - name: Run tests with coverage
        run: |
          poetry run python -m pytest -W error --cov --cov-report=term-missing --cov-report=xml:coverage.xml --junitxml=pytest-junit.xml
          echo "Generated coverage.xml and pytest-junit.xml"

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: backend/coverage.xml
          if-no-files-found: warn

      - name: Upload JUnit test report
        uses: actions/upload-artifact@v4
        with:
          name: backend-junit
          path: backend/pytest-junit.xml
          if-no-files-found: warn
